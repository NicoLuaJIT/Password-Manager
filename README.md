# Password-Manager
Small password manager

# How To Run
Download `password_manager.cpp` and run `g++ password_manager.cpp -o password_manager` and then run `./password_manager` on Linux/macOS.

or `.\password_manager` on Windows.
